// Configurable values shared across the legal/policy pages. These are
// business decisions the store owner should confirm, not legal facts —
// adjust them to match your actual operating policy before launch.

export const policiesConfig = {
  // Return window in days from delivery. 30 days is a common default for
  // DTC ecommerce; confirm this matches your actual policy before launch.
  returnWindowDays: 30,
  lastUpdated: "August 16, 2026",
};
